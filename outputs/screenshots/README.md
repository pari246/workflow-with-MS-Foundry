# Output Screenshots

Add your project evidence screenshots here.

Recommended filenames:

```text
workflow-output
```

The main README already references these filenames.

Before committing screenshots, verify that they do not reveal API keys, access
tokens, credentials, or other sensitive Azure information.
